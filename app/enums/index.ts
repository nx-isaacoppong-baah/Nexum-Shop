export enum StoryVersions {
	draft = "draft",
	published = "published"
};

export enum StoriesEndpoints {
	__init__ = "cdn/stories",
	products = "cdn/stories/Main-product/"
};

export enum Themes {
	dark = "dark",
	light = "light"
}

